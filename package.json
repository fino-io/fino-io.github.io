{
  "name": "fino-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:prepare": "node scripts/sync-aip-english.mjs && node scripts/sync-grpc-english.mjs && node scripts/generate-aip-sidebar.mjs && node scripts/generate-grpc-sidebar.mjs",
    "docs:dev": "npm run docs:prepare && vitepress dev docs",
    "docs:build": "npm run docs:prepare && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "@viz-js/viz": "^3.29.0",
    "vitepress": "^1.6.3",
    "yaml": "^2.9.0"
  }
}
