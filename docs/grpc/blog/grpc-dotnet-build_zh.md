---
title: gRPC 满足 .NET SDK 和 Visual Studio：构建时自动生成代码
spelling: cSpell:ignore autogenerated drwxrwxr classlib globbing Katsnelson Kirill protodep netstandard Nuget walkthrough
date: 2018-06-26
author:
  name: Kirill 'kkm' Katsnelson
  link: https://github.com/kkm000
source_url: https://grpc.io/blog/grpc-dotnet-build/
---

作为 Microsoft 迈向跨平台 .NET 产品的一部分，他们极大地简化了项目文件格式，并允许第三方代码生成器与 .NET 项目紧密集成。我们正在倾听，现在很自豪地从 Grpc.Tools NuGet 包的 1.17 版开始，在 .NET C# 项目中引入 Protocol Buffer 和 gRPC 服务 `.proto` 文件的集成编译，现在可以从 Nuget.org 获取。

<!--more-->

您不再需要使用手写脚本从 `.proto` 文件生成代码：.NET 构建魔法会为您处理这个问题。集成工具可在调用代码生成器之前定位 proto 编译器和 gRPC 插件、标准 Protocol Buffer 导入并跟踪依赖关系，以便生成的 C# 源文件永远不会过时，同时将重新生成保持在所需的最低限度。本质上，`.proto` 文件被视为 .NET C# 项目中的一流源。

## 演练

在这篇博文中，我们将介绍使用跨平台 `dotnet` 命令从 `.proto` 文件创建库的最简单也可能是最常见的场景。我们将本质上实现 `Greeter` 库的克隆，由 [C# `Helloworld` 示例目录](https://github.com/grpc/grpc/tree/master/examples/csharp/Helloworld/Greeter) 中的客户端和服务端项目共享。

### 创建一个新项目

让我们从创建一个新的库项目开始。

```bash
~/work$ dotnet new classlib -o MyGreeter
The template "Class library" was created successfully.

~/work$ cd MyGreeter
~/work/MyGreeter$ ls -lF
total 12
-rw-rw-r-- 1 kkm kkm   86 Nov  9 16:10 Class1.cs
-rw-rw-r-- 1 kkm kkm  145 Nov  9 16:10 MyGreeter.csproj
drwxrwxr-x 2 kkm kkm 4096 Nov  9 16:10 obj/
```

观察到 `dotnet new` 命令创建了我们不需要的文件 `Class1.cs`，因此将其删除。此外，我们还需要一些 `.proto` 文件进行编译。对于本练习，我们将从 gRPC 发行版复制示例文件 [`examples/protos/helloworld.proto` ](https://github.com/grpc/grpc/blob/master/examples/protos/helloworld.proto)。

```bash
~/work/MyGreeter$ rm Class1.cs
~/work/MyGreeter$ wget -q https://raw.githubusercontent.com/grpc/grpc/master/examples/protos/helloworld.proto
```

（在 Windows 上，使用 `del Class1.cs`，并且，如果您没有 wget 命令，只需 [打开上面的 URL ](https://raw.githubusercontent.com/grpc/grpc/master/examples/protos/helloworld.proto) 并从 Web 浏览器使用 *另存为...* 命令）。

接下来，将所需的 NuGet 包添加到项目中：

```bash
~/work/MyGreeter$ dotnet add package Grpc
info : PackageReference for package 'Grpc' version '1.17.0' added to file '/home/kkm/work/MyGreeter/MyGreeter.csproj'.
~/work/MyGreeter$ dotnet add package Grpc.Tools
info : PackageReference for package 'Grpc.Tools' version '1.17.0' added to file '/home/kkm/work/MyGreeter/MyGreeter.csproj'.
~/work/MyGreeter$ dotnet add package Google.Protobuf
info : PackageReference for package 'Google.Protobuf' version '3.6.1' added to file '/home/kkm/work/MyGreeter/MyGreeter.csproj'.
```

### 将 `.proto` 文件添加到项目中

**接下来是重要的部分。** 首先，默认情况下，`.csproj` 项目文件会自动查找其目录中的所有 `.cs` 文件，尽管 [Microsoft 现在建议抑制这种通配行为](https://docs.microsoft.com/dotnet/core/tools/csproj#recommendation)，所以我们也决定不通配 `.proto` 文件。因此，必须将 `.proto` 文件显式添加到项目中。

其次，将属性 `PrivateAssets="All"` 添加到 Grpc.Tools 包引用中非常重要，这样新库的使用者就不会不必要地获取它。这是有道理的，因为该包仅包含编译器、代码生成器和导入文件，在编译 `.proto` 文件的项目之外不需要这些文件。虽然这个简单的演练中没有严格要求，但这必须是您始终这样做的标准做法。

因此，编辑文件 `MyGreeter.csproj` 以添加 `helloworld.proto` 以便对其进行编译，并将 `PrivateAssets` 属性添加到 Grpc.Tools 包引用中。您生成的项目文件现在应如下所示：

```xml
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Google.Protobuf" Version="3.6.1" />
    <PackageReference Include="Grpc" Version="1.17.0" />

    <!-- The Grpc.Tools package generates C# sources from .proto files during
         project build, but is not needed by projects using the built library.
         It's IMPORTANT to add the 'PrivateAssets="All"' to this reference: -->
    <PackageReference Include="Grpc.Tools" Version="1.17.0" PrivateAssets="All" />

    <!-- Explicitly include our helloworld.proto file by adding this line: -->
    <Protobuf Include="helloworld.proto" />
  </ItemGroup>

</Project>
```

### 构建它！

此时，您可以使用 `dotnet build` 命令构建项目来编译 `.proto` 文件和库程序集。在本演练中，我们将向命令添加日志记录开关 `-v:n`，以便我们可以看到编译 `helloworld.proto` 文件的命令实际上已运行。您可能会发现在第一次编译项目时始终这样做是个好主意！

请注意，下面省略了许多输出行，因为构建输出非常详细。

```bash
~/work/MyGreeter$ dotnet build -v:n

Build started 11/9/18 5:33:44 PM.
  1:7>Project "/home/kkm/work/MyGreeter/MyGreeter.csproj" on node 1 (Build target(s)).
   1>_Protobuf_CoreCompile:
      /home/kkm/.nuget/packages/grpc.tools/1.17.0/tools/linux_x64/protoc
        --csharp_out=obj/Debug/netstandard2.0
        --plugin=protoc-gen-grpc=/home/kkm/.nuget/packages/grpc.tools/1.17.0/tools/linux_x64/grpc_csharp_plugin
        --grpc_out=obj/Debug/netstandard2.0 --proto_path=/home/kkm/.nuget/packages/grpc.tools/1.17.0/build/native/include
        --proto_path=. --dependency_out=obj/Debug/netstandard2.0/da39a3ee5e6b4b0d_helloworld.protodep helloworld.proto
     CoreCompile:

        [ ... skipping long output ... ]

       MyGreeter -> /home/kkm/work/MyGreeter/bin/Debug/netstandard2.0/MyGreeter.dll

Build succeeded.
```

如果此时再次调用 `dotnet build -v:n` 命令，则不会调用 `protoc`，并且不会编译任何 C# 源代码。但是，如果更改 `helloworld.proto` 源，则其输出将重新生成，然后在构建过程中由 C# 编译器重新编译。这是您在修改任何源文件时所期望的常规依赖性跟踪行为。

当然，您也可以将 `.cs` 文件添加到同一项目中：毕竟，这是一个构建 .NET 库的常规 C# 项目。这是在我们的 [RouteGuide ](https://github.com/grpc/grpc/tree/master/examples/csharp/RouteGuide/RouteGuide) 示例中完成的。

### 生成的文件在哪里？

您可能想知道 proto 编译器和 gRPC 插件输出 C# 文件在哪里。默认情况下，它们与其他生成的文件（例如对象（在 .NET 构建用语中称为“中间输出”目录））放置在 `obj/` 目录下的同一目录中。这是 .NET 构建的常规做法，这样自动生成的文件就不会弄乱工作目录或意外地置于源代码管理之下。否则，它们可以被调试器等工具访问。您也可以在该目录中看到其他自动生成的源：

```bash
~/work/MyGreeter$ find obj -name '*.cs'
obj/Debug/netstandard2.0/MyGreeter.AssemblyInfo.cs
obj/Debug/netstandard2.0/Helloworld.cs
obj/Debug/netstandard2.0/HelloworldGrpc.cs
```

（如果您从 Windows 命令提示符执行本演练，请使用 `dir /s obj\*.cs`）。

## 还有更多内容

虽然在许多情况下最简单的默认行为就足够了，但有很多方法可以在大型项目中微调 `.proto` 编译过程。如果您发现默认安排不适合您的工作流程，我们鼓励您阅读[文档文件 BUILD-INTEGRATION.md ](https://github.com/grpc/grpc/blob/master/src/csharp/BUILD-INTEGRATION.md) 以获取可用选项。该包还扩展了 Visual Studio 的“属性”窗口，因此您可以在 Visual Studio 界面中为每个文件设置一些选项。

还支持“经典​​”`.csproj` 项目和 Mono。

## 分享您的经验

与复杂功能的任何初始版本一样，我们很高兴收到您的反馈。是不是有什么事情没有按预期进行？您是否有新工具难以覆盖的场景？您知道如何改进总体工作流程吗？请仔细阅读文档，然后在 GitHub 上的 gRPC 代码存储库中[打开问题](https://github.com/grpc/grpc/issues)。您的反馈对于确定我们构建集成工作的未来方向非常重要！
