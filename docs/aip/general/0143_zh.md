---
id: 143
state: approved
lang: zh-CN
source: ./0143.md
title: 'AIP-143：标准化代码'
---

# 标准化代码

语言、国家、货币等常见概念通常已有用于数据通信和处理的标准代码（通常由[国际标准化组织][iso]制定）。使用代码可以避免同一概念有多种文字表达，例如 “United States” 与 “USA”。

## 指导

存在广泛使用的标准代码时，表示该概念的字段**应**在输入和输出中使用标准代码。

```proto
message Book {
  // 表示图书原始语言的 IETF BCP-47 语言代码。
  string language_code = 99;
}
```

- 字段**必须**使用标准要求的数据类型（通常是 `string`）。即使允许值很少，也**不应**使用 enum，因为跨多个 API 使用时会产生令人困惑的查找表。
- 字段**必须**说明所遵循的标准，最好提供标准本身或权威说明的链接。

### 内容类型

表示媒体类型时，**应**使用 [IANA 的媒体类型][iana media types]（MIME 类型），字段名**应**为 `content_type`。

### 国家和地区

国家或地区字段**应**使用 [Unicode CLDR 地区代码][cldr]，字段名**应**为 `region_code`，而不是 `country_code`、`country` 或 `region`。代码列表见[地区信息列表][list]。

### 货币

货币字段**应**使用 [ISO 4217 三字母代码][iso-4217]，字段名**应**为 `currency_code`。金额应结合 [`google.type.Money`][money] 使用。

### 语言

语言字段**应**使用 [IETF BCP-47 语言标签][bcp-47]，字段名**应**为 `language_code`。

### 时区

时区字段**应**使用 [IANA 时区数据库名称][iana tz]，如 `America/Los_Angeles`，字段名**应**为 `time_zone`。
如果字段表示 UTC 偏移量，则**必须**使用 [ISO-8601 格式][iso-8601 format]，字段名**应**为 `utc_offset`。

## 设计缘由

代码使 API 可以跨语言、跨产品一致地表示概念，并避免为同一概念维护多个本地化字符串。字段名应描述代码代表的概念，而不是把代码格式误当成概念本身。

[iso]: https://www.iso.org/
[bcp-47]: https://en.wikipedia.org/wiki/IETF_language_tag
[cldr]: http://cldr.unicode.org/
[iana media types]: https://www.iana.org/assignments/media-types/media-types.xhtml
[iana tz]: http://www.iana.org/time-zones
[iso-4217]: https://en.wikipedia.org/wiki/ISO_4217
[iso-8601 format]: https://en.wikipedia.org/wiki/ISO_8601#Time_offsets_from_UTC
[list]: https://www.unicode.org/cldr/charts/46/supplemental/territory_information.html
[money]: https://github.com/googleapis/googleapis/blob/master/google/type/money.proto

## 变更记录

- **2019-07-24：** 建立标准化代码字段的通用指导。

> 本文译自 [AIP-143 英文原文](./0143.md)。除非另有说明，正文内容采用 [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/) 许可，代码示例采用 [Apache 2.0](https://www.apache.org/licenses/LICENSE-2.0) 许可。
